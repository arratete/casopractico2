- name: Firewall Configuration for NFS
  firewalld:
    service: "{{ item }}"
    permanent: yes
    state: enabled
    immediate: yes
  become: yes
  with_items:
    - "nfs"
    - "rpc-bind"
    - "mountd"